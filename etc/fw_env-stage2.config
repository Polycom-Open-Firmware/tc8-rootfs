# stage-2 (open bootloader) u-boot environment — for the remote fastboot
# one-shot and bootsel tuning (see tc8-firmware-build USING.md). The STOCK
# env config lives in /etc/fw_env.config (0x400000/0x1000).
# device      offset      env_size
/dev/mmcblk2  0x700000    0x4000
