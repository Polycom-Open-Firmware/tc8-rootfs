# TC8 slim rootfs package list.
# One package per line. Blank lines and # comments ignored.
# Installed inside the chroot with `apt-get install --no-install-recommends`.

# init / base
systemd
systemd-sysv
systemd-resolved
systemd-timesyncd
udev
dbus
libnss-systemd

# networking
iproute2
iputils-ping
isc-dhcp-client
openssh-server

# wayland kiosk: compositor + browser
cage
cog
libwpebackend-fdo-1.0-1
libwpewebkit-1.1-0
libwpe-1.0-1
xwayland

# input / display / GPU
seatd
libinput-bin
libegl1
libgles2
mesa-utils

# audio
alsa-utils

# HW video decode (Hantro VPU via v4l2-request)
gstreamer1.0-plugins-base
gstreamer1.0-plugins-good
gstreamer1.0-plugins-bad
gstreamer1.0-libav
v4l-utils

# minimal utils + initramfs source
util-linux
e2fsprogs
psmisc
procps
less
curl
ca-certificates
busybox-static
# polish: silences pam_env warning, persists clock across reboots
# without an RTC (so HTTPS works before NTP catches up)
locales
fake-hwclock

# u-boot env editing from running Linux — lets us tweak tc8_bootargs /
# boot_slot without dropping to the u-boot prompt over serial. Reads via
# /etc/fw_env.config (baked by chroot-setup.sh) which points at the
# eMMC's u-boot env block.
# Debian splits the tools: u-boot-tools ships mkimage + an example
# fw_env.config; libubootenv-tool ships the actual fw_printenv /
# fw_setenv binaries (same names, called via /etc/fw_env.config).
u-boot-tools
libubootenv-tool
